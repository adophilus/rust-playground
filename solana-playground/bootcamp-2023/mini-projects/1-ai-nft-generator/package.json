{
  "name": "nft-ai-generator",
  "version": "0.0.1",
  "scripts": {
    "dev": "pnpm run -r --parallel --aggregate-output dev",
    "dev:db": "pnpm run --filter @nft-ai-generator/backend dev:db",
    "build": "pnpm run -r build"
  }
}
